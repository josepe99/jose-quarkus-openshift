apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: replicas.yaml
  - path: images.yaml

secretGenerator:
  - name: postgres-secret
    envs:
      - .env

generatorOptions:
  disableNameSuffixHash: true
